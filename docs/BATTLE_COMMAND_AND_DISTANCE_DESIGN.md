# 战术指挥系统与动态战场距离设计规范

**文档版本**：`battle-tactics-v1-20260919`  
**核心定位**：战中实时指挥微操、两阶段回合制战棋结算与自适应战场距离体系。

---

## 一、 设计背景与核心哲学

在早期版本中，战场距离采用静态固定的 `2200`（野地战斗）与 `3000`（城战/对战）。这种静态设计带来了诸多体验与数值矛盾：
1. **垃圾跑图回合严重**：3000 距离下加兰德步兵（速度 3，步长 150）需要对走近 10 个回合才能接敌；而在攻打只有固定工事（速度 0）的城池时，攻方步兵需要单向跑 18 个回合，极易触发 30 回合超时判负。
2. **战术指挥空间缺失**：纯数值自动推进碰撞，玩家无法在战中进行拉扯、选位、集火和避险。
3. **兵种移速尺度割裂**：早期特种兵移速被误设为 13（比战斗机还快），十轮卡车移速为 8（比轻坦还快），导致混编部队一踩油门便瞬间脱节。

**本方案确立的核心设计哲学**：
> **“让距离服务于战术情境（Context-Driven），消灭无意义的垃圾跑图回合（Fast Engagement），并赋予玩家调控交火距离与拉扯走位的策略博弈空间（Tactical Agency）。”**

---

## 二、 动态战场距离系统（3 回合遭遇制）

### 1. 动态距离数学公式
战场初始距离 $D$ 不再使用硬编码常数，而是依据交战双方的**最高机动速度**与**全场最远有效射程**动态自适应生成：

$$D = 3 \times (v_A^{\max} + v_B^{\max}) \times 50 + \max(\text{effectiveRange})$$

*   $v_A^{\max}$：攻方所有参战存活单位中的最高有效速度（含引擎科技与军官技能加成）。
*   $v_B^{\max}$：守方所有参战存活单位中的最高有效速度。当防守方仅有城防工事时，$v_B^{\max} = 0$。
*   $\max(\text{effectiveRange})$：全场攻守双方所有存活单位中的最高武器有效射程（含科技加成）。
*   $50$：基础步长换算系数（单位推进步长 $\text{step} = \text{spd} \times 50$）。

### 2. 战术时间轴与交战节奏
*   **第 1～3 回合（战前机动与部署期，黄金 3 回合）**：
    *   双方全速对进或调整站位，在此期间全场**没有任何武器处于有效射程之内**；
    *   没有任何单位可以提前开火偷伤害，给玩家提供完整的侦察、预判、微操与布阵窗口。
*   **第 4 回合初（首次交火点火）**：
    *   前锋高速单位或超远程火炮（如喀秋莎 760 射程）正好在第 4 回合初跨入射程临界点，战役第一枪准时打响！
*   **后续回合（梯次全面交战）**：
    *   重装坦克与中坚部队在第 5～6 回合撞线接火；
    *   步兵大队在第 6～7 回合拔枪射击，全军进入深水区博弈，节奏紧凑明快。

### 3. 攻城战距离的自然收敛
攻打纯工事城防时，守军速度为 0。攻方单向冲刺 3 回合走完前沿距离，初始战场距离自动收敛到 **1200 ~ 1500** 之间。步兵大队 7 回合即可逼近城防碉堡展开登城攻坚，彻底终结了“攻城跑马拉松超时”的死局。

---

## 三、 战场空间与“阵地底线”物理规则

```
攻方阵地底线 (X = 0) <──────────────── 初始距离 D ────────────────> 守方阵地底线 (X = D)
* 攻方不可后退越过 0                                                 * 守方不可后退越过 D
```

1. **一维坐标轴**：
   - 攻方阵地基准线定在坐标 `0`，向前推进为正方向（$+X$）；
   - 守方阵地基准线定在坐标 $D$，向前推进为负方向（$-X$）；
   - 攻守双方任意两单位之间的相对物理距离为：$\text{Distance} = \max(0, \text{enemyX} - \text{mineX})$。
2. **阵地底线（Baseline Boundary）**：
   - 攻方坐标必须满足 $\text{pos} \ge 0$，守方坐标必须满足 $\text{pos} \le D$；
   - 开战初始，双方所有部队均位于各自阵地底线（攻方坐标统一为 `0`，守方坐标统一为 $D$），**开局点击【后退】无法向后移动**（已触碰底线）；
   - 这一规则从物理上彻底封死了“无限倒车风筝拖平局”的恶性漏洞。
3. **取消开局前置偏移（Formation Offset = 0）**：
   - 开战起点完全平齐对齐，取消任何兵种的开局前置坐标偏移；
   - 重坦（IS-2）与其他单位同一起点出发，其前排掩护效果完全依赖战中机动与站位距离动态判定。

---

## 四、 全兵种移动速度（spd）与步长矩阵

全兵种速度收敛在合理的机械化物理梯队中，单回合步长统一遵循 $\text{step} = \text{spd} \times 50$：

| 兵种 / 工事标识 | 兵种全称 | 领域 | 移速（spd） | 单回合步长 | 基础射程 | 核心战术定位与特性 |
| :--- | :--- | :---: | :---: | :---: | :---: | :--- |
| `infantry` | 加兰德步兵（M1） | 陆 | **3** | **150** | 100 | **阵线中坚**：最慢步巡单位，近战肉搏与廉价填线 |
| `htank` | 斯大林重型坦克（IS-2） | 陆 | **4** | **200** | **320** | **钢铁长城**：420血/60防，320视距直瞄压制，为身后提供掩护 |
| `assault` | 自行加榴突击炮（ISU-152） | 陆 | **4** | **200** | **750** | **战役攻坚**：152毫米重炮超长距拔点攻坚 |
| `rocket` | 喀秋莎火箭炮（BM-13） | 陆 | **5** | **250** | **2000** | **战略炮皇**：超视距大纵深火箭覆盖洗地 |
| `sub` | 小鲨鱼级潜艇（Gato） | 海 | **5** | **250** | **100** | **深海刺客**：潜行贴脸100距离，重型直航鱼雷绝杀 |
| `ltank` | 斯图亚特轻型坦克（M5A1） | 陆 | **6** | **300** | 220 | **机动突击**：快速履带反炮兵尖刀，穿插侧翼 |
| `truck` | 十轮大卡车（CCKW-353） | 陆 | **6** | **300** | 0 | **地面后勤（载重50）**：大地图高速掠夺，战内默认后排待命 |
| `battleship` | 衣阿华级战列舰（Iowa） | 海 | **6** | **300** | **1600** | **巨舰重炮**：406毫米主炮，超远距海面火力倾泻 |
| `carrier` | 埃塞克斯级航母（Essex） | 海 | **6** | **300** | **1900** | **大洋王牌**：超视距舰载机群战略轰炸与夺取制海空权 |
| `armored` | 猎鹿犬装甲车（T17E2） | 陆 | **7** | **350** | 300 | **轮式猎手**：前沿游弋侦查，反步兵兼机动防空 |
| `motor` | 哈雷摩托兵（WLA） | 陆 | **7** | **350** | 140 | **极速轻骑**：双轮极速穿插，近战侧翼袭扰 |
| `destroyer` | 弗莱彻级驱逐舰（Fletcher） | 海 | **7** | **350** | **400** | **带刀护卫**：前沿中口径舰炮，防空护航与外围雷达哨 |
| `special` | 英国突击队特种兵（Commando）| 陆 | **8** | **400** | 180 | **地面第一刺客**：高速强突，无视重坦掩护秒后排，打了就跑 |
| `transport` | 空中列车运输机（C-47） | 空 | **8** | **400** | 0 | **空中物流（载重80）**：纯空军高速大劫掠专用 |
| `bomber` | 飞行堡垒轰炸机（B-17G） | 空 | **8** | **400** | 300 | **战略重锤**：与运输机同速，空袭主力 |
| `fighter` | 野马战斗机（P-51） | 空 | **10** | **500** | **350** | **极速制空**：全军最高机动，争夺战区制空权 |
| `scout` | 闪电侦察机（F-5） | 空 | **11** | **550** | 200 | **空天侦查**：大地图极致行军开眼，战内默认待命 |
| `bunker` | 碉堡 | 工事 | **0** | **0** | **1050** | 固定工事，前沿高硬度反步兵机枪掩体火网 |
| `howitzer` | 榴弹炮 | 工事 | **0** | **0** | **3950** | 固定工事，超远程要塞曲射重炮，全图火力压制与岸防 |
| `antitank` | 反坦克炮 | 工事 | **0** | **0** | **3850** | 固定工事，超远重型反装甲穿甲炮，封锁坦克冲锋走廊与岸防 |
| `flak` | 防空炮 | 工事 | **0** | **0** | **1850** | 固定工事，超远高空密集防空弹幕，截击战机与轰炸机 |

---

## 五、 两阶段回合制结算引擎（Phase-based Execution）

为了彻底解决旧逻辑中“单兵种边移动边开火导致后续顺位兵种距离失真”的硬伤，回合结算重构为规范的**两阶段（Two-Phase）模型**：

```
       ┌─────────────────────────────────────────────────────────────┐
       │             第 R 回合：接收双方指令 (或读取默认托管)         │
       │             指令集: 前进(ADVANCE) / 后退(RETREAT) /         │
       │                     待命(HOLD)    / 目标(focusTarget)       │
       └──────────────────────────────┬──────────────────────────────┘
                                      │
                                      ▼
       ┌─────────────────────────────────────────────────────────────┐
       │ 【第一阶段：统一机动阶段 (Movement Phase)】                 │
       │ 1. 攻守双方所有存活单位同时结算位移:                         │
       │    攻方: 前进 +step, 后退 -step, 边界约束 [0, D]             │
       │    守方: 前进 -step, 后退 +step, 边界约束 [0, D]             │
       │ 2. 刷新全场各兵种坐标字典 (minePos, enemyPos)               │
       │ 3. 记录机动日志 (如: 后退 400 距离->1800)                    │
       └──────────────────────────────┬──────────────────────────────┘
                                      │
                                      ▼
       ┌─────────────────────────────────────────────────────────────┐
       │ 【第二阶段：战术交火阶段 (Combat Phase)】                   │
       │ 1. 行动序列排序: 全场单位按 [有效射程降序, 速度降序] 排序   │
       │ 2. 目标索敌:                                                │
       │    a. 若玩家指定了 focusTarget 且该目标在射程内，优先集火   │
       │    b. 否则按最优相克倍率、距离、生命与重坦掩护规则索敌      │
       │ 3. 开火结算 (边打边退):                                     │
       │    即使本回合执行了【后退】，只要当前物理距离 <= 射程，     │
       │    该单位依然打出完整齐射火力并计算伤亡                     │
       │ 4. 实时扣除伤亡，阵亡单位移出战场                           │
       └─────────────────────────────────────────────────────────────┘
```

### “边打边退（Hit-and-Run）”风筝机制
*   单位在执行【后退】时，第一阶段先拉开 $\text{spd} \times 50$ 的距离；
*   在第二阶段开火时，若拉开距离后的最新距离仍在其射程内，单位正常打出攻击；
*   若敌人移速更快并在第一阶段贴近，后撤未能拉脱射程，双方将在新距离交火；
*   这种机制赋予了特种兵（速度 8）、喀秋莎火箭（射程 760）、装甲车（速度 7）极佳的操作上限。

---

## 六、 战报指令还原规范

战报中严格记录每回合各兵种的战术机动与交火详情，便于玩家查看回放与复盘战术：

1. **移动日志模板**：
   - 前进：`我方 特种兵(50) 前进 400 距离->1200`
   - 后退（边打边退）：`我方 特种兵(50) [后退 400|边打边退] 距离->1600`
   - 待命：`我方 喀秋莎(20) 待命 距离->2100`
   - 触底无法后退：`我方 加兰德步兵(100) 后退受阻(已至底线) 距离->2400`
2. **交火日志模板**：
   - 优先集火指定目标：`我方 特种兵(50) 突袭 敌 喀秋莎(20) [指定集火|倍率×2.0 相克] 对工事攻击188 伤害1200 击毁15 ...`
   - 常规开火：`我方 重型坦克(30) 炮击 敌 轻型坦克(50) [倍率×2.25 相克] 对地攻击50 伤害2500 击毁15 ...`
