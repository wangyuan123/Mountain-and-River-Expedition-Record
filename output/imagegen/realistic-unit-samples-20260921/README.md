# 写实兵种模型样稿

状态：密钥已配置于工作区之外；已尝试官方 CLI 生成，但连接 OpenAI API 失败，尚无新图片。沙箱外网络诊断结果为 TLS 连接被重置（Connection reset by peer）。网络恢复后可使用 batch.jsonl 重试。

生成方式：imagegen 官方 CLI 备用流程，gpt-image-2，1024×1024，high。

四款独立样稿：P-51D、B-17G、IS-2、Iowa。完整提示词分别保存在同目录的 .prompt.txt 文件中；batch.jsonl 可用于批量调用。
